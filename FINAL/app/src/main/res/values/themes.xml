<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="Theme.Gastos" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Colores Primarios (para botones y acentos por defecto) -->
        <item name="colorPrimary">@color/neon_purple</item>
        <item name="colorPrimaryVariant">@color/bg_card</item>
        <item name="colorOnPrimary">@color/white</item>

        <!-- Color de fondo y barra de estado -->
        <item name="android:windowBackground">@color/bg_main</item>
        <item name="android:statusBarColor">@color/bg_main</item>
    </style>
</resources>